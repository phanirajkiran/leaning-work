[filter1]
summary=All defects opened by kernel testers  in 8.0
filter=reporter in (HChen1,LLiu2) AND project = LIN8 AND summary !~ Security AND issuetype = Bug  ORDER BY Component DESC, reporter DESC
