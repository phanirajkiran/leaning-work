[filter1]
summary=The defect opened in 8.0
filter=project = LIN8 AND summary !~ "Security" AND issuetype = Bug AND resolution = Unresolved ORDER BY priority DESC
[filter2]
summary=NEW BSP defects opened today in LIN8 
filter=project = LIN8  AND component = BSP AND created >= -16h ORDER BY assignee ASC,priority DESC
