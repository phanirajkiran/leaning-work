[filter1]
summary=Please check below KTS test case defects, and fix them this week 
filter=project = LIN9 AND issuetype = Bug AND component = Test AND reporter in (LChi1) AND status not in ('Resolved','Closed') AND text ~ "KTS" order by created DESC
